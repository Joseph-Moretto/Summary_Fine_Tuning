# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
.env

# Editors
.vscode/
.idea/
*.swp

# SLURM output (scripts write here when submitted from the repo root)
logs/*
!logs/README.md

# Model weights and trainer checkpoints. Every adapter_model.safetensors from
# this project is over GitHub's 100 MB file limit; adapter_config.json and
# the tokenizer files are kept.
*.safetensors
checkpoint-*/
merged/
runs/

# Hugging Face cache, if kept inside the repo
hf_cache/
